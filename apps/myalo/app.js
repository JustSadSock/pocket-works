import{capturePointer,installMobileRuntime,releasePointer}from'../../shared/mobile-runtime.js';import{createWorkshopMode}from'../../shared/workshop-mode.js';import{watchConnectivity}from'../../shared/pwa-utils.js';import{ElasticRenderer}from'./elastic-renderer.js';import{HAND_CONNECTIONS,createFeatureNodes,grabFeature,moveGrabbedFeature,nearestFeature,releaseFeature,stepFeaturePhysics,updateFeatureTargets}from'./feature-map.js';import{MyaloAudio}from'./audio.js';const APP_VERSION='1.0.0';const STORAGE_KEY='pocket-works:myalo:preferences';const TRACK_INTERVAL=1000/25;const FACE_FADE_MS=700;const MODEL_CACHE='myalo-v1.0.0';const MODEL_ASSETS=['https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/vision_bundle.mjs','https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/wasm/vision_wasm_module_internal.js','https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/wasm/vision_wasm_module_internal.wasm','https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task','https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task'];installMobileRuntime();const mirror=document.querySelector('#mirror');const video=document.querySelector('#camera');const scene=document.querySelector('#scene');const overlay=document.querySelector('#overlay');const startup=document.querySelector('#startup');const startButton=document.querySelector('#start-button');const retryButton=document.querySelector('#retry-button');const permissionError=document.querySelector('#permission-error');const errorTitle=document.querySelector('#error-title');const errorCopy=document.querySelector('#error-copy');const loadingRail=document.querySelector('#loading-rail');const loadingLabel=document.querySelector('#loading-label');const loadingFill=document.querySelector('#loading-fill');const liveStatus=document.querySelector('#live-status');const statusCopy=document.querySelector('#status-copy');const instruction=document.querySelector('#instruction');const instructionCopy=document.querySelector('#instruction-copy');const pointsToggle=document.querySelector('#points-toggle');const soundToggle=document.querySelector('#sound-toggle');const preferences=loadPreferences();const nodes=createFeatureNodes();const audio=new MyaloAudio(preferences.sound);let renderer;let tracker;let cameraStream=null;let processorCanvas=null;let processorContext=null;let workerReady=false;let workerBusy=false;let running=false;let starting=false;let lastFrameSentAt=0;let lastRenderAt=performance.now();let lastFaceAt=0;let latestFace=[];let latestHands=[];let latestHandedness=[];let directGrab=null;let hadFace=false;let online=navigator.onLine;let reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const handStates=new Map();try{renderer=new ElasticRenderer(scene,video);}catch(error){showFatal('WebGL недоступен','Для живой деформации нужен WebGL. Открой приложение в Safari или Chrome без режима энергосбережения.');}pointsToggle.setAttribute('aria-pressed',String(preferences.points));soundToggle.setAttribute('aria-pressed',String(preferences.sound));function loadPreferences(){try{const stored=JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}');return{points:stored.points!==false,sound:stored.sound!==false};}catch{return{points:true,sound:true};}}function savePreferences(){localStorage.setItem(STORAGE_KEY,JSON.stringify(preferences));}function setLoading(progress,label){loadingRail.hidden=false;loadingLabel.textContent=label;loadingFill.style.width=`${Math.max(5, Math.min(100, progress * 100))}%`;}function showFatal(title,copy){starting=false;running=false;startup.hidden=true;loadingRail.hidden=true;liveStatus.hidden=true;instruction.hidden=true;errorTitle.textContent=title;errorCopy.textContent=copy;permissionError.hidden=false;}function friendlyCameraError(error){if(!navigator.mediaDevices?.getUserMedia){return['Камера не поддерживается','Этот браузер не даёт веб-приложениям доступ к камере. Открой МЯЛО в Safari или Chrome.'];}if(error?.name==='NotAllowedError'||error?.name==='SecurityError'){return['Доступ к камере запрещён','Разреши камеру для этого сайта в настройках браузера, затем нажми «Повторить».'];}if(error?.name==='NotFoundError'||error?.name==='OverconstrainedError'){return['Фронтальная камера не найдена','Не удалось найти доступную камеру. Проверь ограничения устройства и повтори попытку.'];}if(error?.name==='NotReadableError'||error?.name==='AbortError'){return['Камера занята','Закрой другое приложение, использующее камеру, и попробуй снова.'];}return['Не удалось открыть камеру',error?.message||'Проверь разрешение камеры и повтори попытку.'];}async function waitForVideo(){if(video.readyState>=2&&video.videoWidth)return;await new Promise((resolve,reject)=>{const timeout=setTimeout(()=>reject(new Error('Камера не отдала изображение')),10000);const onReady=()=>{clearTimeout(timeout);video.removeEventListener('loadeddata',onReady);resolve();};video.addEventListener('loadeddata',onReady,{once:true});});}async function openCamera(){setLoading(0.04,'Запрашиваю фронтальную камеру');const stream=await navigator.mediaDevices.getUserMedia({audio:false,video:{facingMode:{ideal:'user'},width:{ideal:1280},height:{ideal:960},frameRate:{ideal:30,max:30}}});cameraStream=stream;video.srcObject=stream;await video.play();await waitForVideo();renderer?.rebuildLayout();setupProcessor();}function setupProcessor(){const sourceWidth=Math.max(1,video.videoWidth);const sourceHeight=Math.max(1,video.videoHeight);const width=Math.min(560,sourceWidth);const height=Math.max(1,Math.round(width*sourceHeight/sourceWidth));processorCanvas=document.createElement('canvas');processorCanvas.width=width;processorCanvas.height=height;processorContext=processorCanvas.getContext('2d',{alpha:false,desynchronized:true});}async function warmModelCache(){if(!('caches'in window))return;const cache=await caches.open(MODEL_CACHE);let completed=0;for(const url of MODEL_ASSETS){const cached=await cache.match(url);if(!cached){const response=await fetch(url,{mode:'cors',credentials:'omit'});if(!response.ok)throw new Error(`Не удалось скачать модель (${response.status})`);await cache.put(url,response.clone());}completed+=1;setLoading(0.16+completed/MODEL_ASSETS.length*0.34,`Сохраняю зрение ${completed}/${MODEL_ASSETS.length}`);}}function createTracker(){tracker?.terminate();tracker=new Worker('./tracker-worker.js',{type:'module'});tracker.addEventListener('message',handleTrackerMessage);tracker.addEventListener('error',(event)=>{workerBusy=false;showFatal('Трекер упал',event.message||'Не удалось запустить распознавание лица и рук.');});tracker.postMessage({type:'init'});}function handleTrackerMessage(event){const data=event.data;if(data?.type==='progress'){setLoading(0.16+data.progress*0.8,data.label||'Загружаю модели');return;}if(data?.type==='delegate-fallback')return;if(data?.type==='ready'){workerReady=true;workerBusy=false;loadingRail.hidden=true;liveStatus.hidden=false;instruction.hidden=false;statusCopy.textContent='Ищу лицо';liveStatus.dataset.state='search';running=true;starting=false;return;}if(data?.type==='results'){workerBusy=false;latestFace=Array.isArray(data.face)?data.face:[];latestHands=Array.isArray(data.hands)?data.hands:[];latestHandedness=Array.isArray(data.handedness)?data.handedness:[];if(latestFace.length)lastFaceAt=performance.now();updateTrackingState(data.timestamp||performance.now());return;}if(data?.type==='frame-error'){workerBusy=false;return;}if(data?.type==='init-error'){workerBusy=false;const suffix=online?'Проверь, не блокирует ли браузер загрузку моделей, и повтори.':'Первый запуск требует интернет, чтобы один раз сохранить модели на устройстве.';showFatal('Не удалось загрузить зрение',`${data.message || 'Ошибка моделей'}. ${suffix}`);}}async function start(){if(starting||running)return;starting=true;workerReady=false;startButton.disabled=true;retryButton.disabled=true;permissionError.hidden=true;startup.hidden=true;setLoading(0.02,'Поднимаю зеркало');await audio.unlock();try{await openCamera();setLoading(0.14,'Камера готова');await warmModelCache();createTracker();}catch(error){stopCamera();const[title,copy]=friendlyCameraError(error);showFatal(title,copy);}finally{startButton.disabled=false;retryButton.disabled=false;}}function stopCamera(){for(const track of cameraStream?.getTracks?.()||[])track.stop();cameraStream=null;video.srcObject=null;}function sendTrackingFrame(now){if(!running||!workerReady||workerBusy||!processorContext||document.hidden)return;if(now-lastFrameSentAt<TRACK_INTERVAL||video.readyState<2)return;lastFrameSentAt=now;processorContext.save();processorContext.setTransform(1,0,0,1,0,0);processorContext.drawImage(video,0,0,processorCanvas.width,processorCanvas.height);processorContext.restore();workerBusy=true;createImageBitmap(processorCanvas).then((bitmap)=>tracker.postMessage({type:'frame',bitmap,timestamp:now},[bitmap])).catch(()=>{workerBusy=false;});}function distance(a,b){return Math.hypot(a.x-b.x,a.y-b.y);}function handOwner(handedness,index){return`hand:${handedness || 'unknown'}:${index}`;}function updateTrackingState(timestamp){const hasFace=latestFace.length>=468;updateFeatureTargets(nodes,latestFace,(point)=>renderer.mapLandmarkToScreen(point));hadFace=hasFace;const seenOwners=new Set();let activeLabel=null;let pinchingCount=0;latestHands.forEach((hand,index)=>{if(!Array.isArray(hand)||hand.length<21)return;const owner=handOwner(latestHandedness[index],index);seenOwners.add(owner);const thumb=hand[4];const finger=hand[8];const palmA=hand[5];const palmB=hand[17];if(!thumb||!finger||!palmA||!palmB)return;const pinchDistance=distance(thumb,finger);const palmWidth=Math.max(0.035,distance(palmA,palmB));const ratio=pinchDistance/palmWidth;const midpoint=renderer.mapLandmarkToScreen({x:(thumb.x+finger.x)*0.5,y:(thumb.y+finger.y)*0.5});const state=handStates.get(owner)||{pinching:false,node:null,midpoint};const isPinching=state.pinching?ratio<0.48:ratio<0.34;state.midpoint=midpoint;if(isPinching){pinchingCount+=1;if(!state.pinching){state.pinching=true;state.node=nearestFeature(nodes,midpoint,0.125);if(state.node){grabFeature(state.node,owner,midpoint,timestamp);audio.grab(state.node.id);navigator.vibrate?.(12);}}if(state.node){moveGrabbedFeature(state.node,midpoint,timestamp);activeLabel=state.node.label;}}else if(state.pinching){state.pinching=false;if(state.node){const strength=releaseFeature(state.node);audio.release(strength);navigator.vibrate?.(strength>0.42?[9,18,9]:7);}state.node=null;}handStates.set(owner,state);});for(const[owner,state]of handStates){if(seenOwners.has(owner))continue;if(state.node){const strength=releaseFeature(state.node);audio.release(strength);}handStates.delete(owner);}if(!hasFace){liveStatus.dataset.state='search';statusCopy.textContent='Ищу лицо';instructionCopy.textContent='Держи лицо в центре кадра и дай зеркалу секунду';}else if(activeLabel){liveStatus.dataset.state='grab';statusCopy.textContent=`Тянешь: ${activeLabel}`;instructionCopy.textContent='Отпусти щепотку — точка вернётся с инерцией';}else if(!latestHands.length){liveStatus.dataset.state='ready';statusCopy.textContent='Лицо найдено';instructionCopy.textContent='Покажи руку и сведи большой палец с указательным';}else if(pinchingCount){liveStatus.dataset.state='ready';statusCopy.textContent='Щепотка';instructionCopy.textContent='Наведи сведённые пальцы прямо на светящуюся точку';}else{liveStatus.dataset.state='ready';statusCopy.textContent='Рука найдена';instructionCopy.textContent='Сожми большой и указательный пальцы на точке';}}function canvasPoint(event){const rect=mirror.getBoundingClientRect();return{x:(event.clientX-rect.left)/Math.max(1,rect.width),y:(event.clientY-rect.top)/Math.max(1,rect.height)};}function onPointerDown(event){if(!running||event.button>0||event.target.closest('button, a'))return;const point=canvasPoint(event);const node=nearestFeature(nodes,point,0.12);if(!node)return;event.preventDefault();const owner=`pointer:${event.pointerId}`;directGrab={pointerId:event.pointerId,owner,node};capturePointer(mirror,event.pointerId);grabFeature(node,owner,point,event.timeStamp||performance.now());audio.unlock().then(()=>audio.grab(node.id));liveStatus.dataset.state='grab';statusCopy.textContent=`Тянешь: ${node.label}`;}function onPointerMove(event){if(!directGrab||directGrab.pointerId!==event.pointerId)return;event.preventDefault();moveGrabbedFeature(directGrab.node,canvasPoint(event),event.timeStamp||performance.now());}function finishPointer(event){if(!directGrab||directGrab.pointerId!==event.pointerId)return;const grab=directGrab;directGrab=null;const strength=releaseFeature(grab.node);audio.release(strength);releasePointer(mirror,event.pointerId);liveStatus.dataset.state=hadFace?'ready':'search';}function resizeOverlay(){const rect=overlay.getBoundingClientRect();const dpr=Math.min(2,window.devicePixelRatio||1);const width=Math.max(1,Math.round(rect.width*dpr));const height=Math.max(1,Math.round(rect.height*dpr));if(overlay.width!==width||overlay.height!==height){overlay.width=width;overlay.height=height;}return{width:rect.width,height:rect.height,dpr};}function drawOverlay(){const context=overlay.getContext('2d');const{width,height,dpr}=resizeOverlay();context.setTransform(dpr,0,0,dpr,0,0);context.clearRect(0,0,width,height);if(!running||!preferences.points)return;const now=performance.now();const staleFace=now-lastFaceAt>FACE_FADE_MS;context.lineCap='round';context.lineJoin='round';if(!staleFace){for(const node of nodes){if(!node.visible||node.confidence<0.08)continue;const x=(node.base.x+node.offset.x)*width;const y=(node.base.y+node.offset.y)*height;const baseX=node.base.x*width;const baseY=node.base.y*height;const grabbed=node.grabbedBy!==null;const displaced=Math.hypot(node.offset.x,node.offset.y)>0.003;if(displaced){context.beginPath();context.moveTo(baseX,baseY);context.lineTo(x,y);context.strokeStyle=grabbed?'rgba(184,255,104,.9)':'rgba(242,238,227,.34)';context.lineWidth=grabbed?2:1;context.stroke();context.beginPath();context.arc(baseX,baseY,2.3,0,Math.PI*2);context.fillStyle='rgba(242,238,227,.38)';context.fill();}context.beginPath();context.arc(x,y,grabbed?9:4.2,0,Math.PI*2);context.fillStyle=grabbed?'#b8ff68':'rgba(242,238,227,.86)';context.fill();context.strokeStyle=grabbed?'rgba(23,24,22,.9)':'rgba(23,24,22,.58)';context.lineWidth=grabbed?2:1;context.stroke();if(grabbed){context.font='800 11px ui-monospace, monospace';context.textAlign='center';context.textBaseline='bottom';context.fillStyle='#b8ff68';context.fillText(node.label.toUpperCase(),x,y-14);}}}latestHands.forEach((hand,handIndex)=>{if(!Array.isArray(hand)||hand.length<21)return;const points=hand.map((point)=>renderer.mapLandmarkToScreen(point));context.beginPath();for(const[a,b]of HAND_CONNECTIONS){const pa=points[a];const pb=points[b];context.moveTo(pa.x*width,pa.y*height);context.lineTo(pb.x*width,pb.y*height);}context.strokeStyle='rgba(184,255,104,.48)';context.lineWidth=1.4;context.stroke();const thumb=points[4];const finger=points[8];const owner=handOwner(latestHandedness[handIndex],handIndex);const state=handStates.get(owner);const midpoint=state?.midpoint||{x:(thumb.x+finger.x)*0.5,y:(thumb.y+finger.y)*0.5};context.beginPath();context.arc(midpoint.x*width,midpoint.y*height,state?.pinching?12:7,0,Math.PI*2);context.strokeStyle=state?.pinching?'#b8ff68':'rgba(242,238,227,.65)';context.lineWidth=state?.pinching?2.4:1.2;context.stroke();});}function frame(now){const dt=(now-lastRenderAt)/1000;lastRenderAt=now;stepFeaturePhysics(nodes,dt,reduceMotion);renderer?.render(nodes,Boolean(cameraStream));drawOverlay();sendTrackingFrame(now);requestAnimationFrame(frame);}startButton.addEventListener('click',start);retryButton.addEventListener('click',start);pointsToggle.addEventListener('click',()=>{preferences.points=!preferences.points;pointsToggle.setAttribute('aria-pressed',String(preferences.points));savePreferences();});soundToggle.addEventListener('click',async()=>{preferences.sound=!preferences.sound;soundToggle.setAttribute('aria-pressed',String(preferences.sound));audio.setEnabled(preferences.sound);if(preferences.sound)await audio.unlock();savePreferences();});mirror.addEventListener('pointerdown',onPointerDown);mirror.addEventListener('pointermove',onPointerMove);mirror.addEventListener('pointerup',finishPointer);mirror.addEventListener('pointercancel',finishPointer);mirror.addEventListener('lostpointercapture',finishPointer);window.matchMedia('(prefers-reduced-motion: reduce)').addEventListener('change',(event)=>{reduceMotion=event.matches;});window.addEventListener('resize',()=>renderer?.rebuildLayout(),{passive:true});window.addEventListener('orientationchange',()=>setTimeout(()=>renderer?.rebuildLayout(),120),{passive:true});document.addEventListener('visibilitychange',()=>{if(document.hidden)workerBusy=false;});window.addEventListener('pagehide',()=>{stopCamera();tracker?.postMessage({type:'dispose'});tracker?.terminate();});watchConnectivity((isOnline)=>{online=isOnline;document.documentElement.dataset.network=isOnline?'online':'offline';});createWorkshopMode({appName:'МЯЛО',version:APP_VERSION,cachePrefix:'myalo-',storageNamespace:'pocket-works:myalo',onReset(){localStorage.removeItem(STORAGE_KEY);preferences.points=true;preferences.sound=true;pointsToggle.setAttribute('aria-pressed','true');soundToggle.setAttribute('aria-pressed','true');audio.setEnabled(true);}});requestAnimationFrame(frame);
