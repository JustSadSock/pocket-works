{
  "name": "@pocket-works/melt-crypt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node source/core.test.mjs && vite build --config vite.config.ts && node ../../scripts/promote-enhanced-build.mjs melt-crypt",
    "test": "node source/core.test.mjs && vitest run --passWithNoTests --config vite.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@babylonjs/core": "^9.2.1"
  }
}
