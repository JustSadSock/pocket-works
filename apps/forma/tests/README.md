# FORMA tests

Run the deterministic CAD/compiler checks with:

```bash
node --test apps/forma/tests/scad-contract.test.mjs
```

The suite verifies a real 5× planetary relation, external gear centre distance, direction errors, fake differential rejection, and unverified housing-containment rejection.
