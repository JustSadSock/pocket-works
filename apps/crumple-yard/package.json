{
  "name": "@pocket-works/crumple-yard",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vitest run --config vite.config.ts && tsc --noEmit -p tsconfig.json && vite build --config vite.config.ts && node ../../scripts/promote-enhanced-build.mjs crumple-yard",
    "test": "vitest run --config vite.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@babylonjs/core": "^9.2.1",
    "@dimforge/rapier3d-compat": "^0.20.0"
  }
}
