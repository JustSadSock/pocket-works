# SIROCCO 1.2 validation

Temporary validation marker for the 1.2.0 animated-character build pass. This branch exists only to force the repository's deploy-preview/build integration to exercise the current `main` app state including the build-time CC0 GLB fetch and Babylon glTF loader.
