# __APP_NAME__

Enhanced Pocket Works application generated with Vite, TypeScript, Workbox and Vitest.

## Runtime

- Preset: `__APP_PRESET__`
- Version: `__APP_VERSION__`
- Storage namespace: `__APP_STORAGE_NAMESPACE__`
- Cache identity: `__APP_CACHE_VERSION__`

## Commands

```bash
npm run build --workspace=@pocket-works/__APP_SLUG__
npm run test --workspace=@pocket-works/__APP_SLUG__
npm run typecheck --workspace=@pocket-works/__APP_SLUG__
```

Source files live in `source/`. The build writes deployable `index.html`, `app.js`, `styles.css`, `manifest.webmanifest` and `sw.js` into this application directory. Do not edit generated build output directly.
