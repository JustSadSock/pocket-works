int:'купить тишину',effect:{treasury:-10,locals:6,planters:2,unrest:-5}},
  {label:'Признать новые границы',hint:'инвестиции получат землю',effect:{production:7,inequality:8,locals:-13,unrest:7}}
 ]},
 audit:{title:'Красный пароход',text:`Из Аурелии пришёл аудиторский пароход. На борту — три ревизора и копии ваших прошлых депеш.`,actor:s.characters.find(c=>c.faction==='bureaucrats')?.id,choices:[
  {label:'Открыть книги',hint:'правда ударит по накопленной лжи',effect:{suspicion:-10,metropoleFavor:Math.max(-18,-s.discrepancy*.35),discrepancy:-18}},
  {label:'Подготовить удобные книги',hint:'сильная ставка на канцелярию',effect:{treasury:-6,corruption:8,suspicion:10,actorRel:8,delayed:{after:3,kind:'audit_echo'}}},
  {label:'Утопить ревизоров в церемониях',hint:'покупка времени',effect:{treasury:-8,suspicion:4,metropoleFavor:4,authority:-4}}
 ]},
 charter:{title:'Письмо без печати',text:`Влиятельные семьи предлагают проект Велорийской хартии: местный совет получит налоги, а вы — право формировать правительство без Аурелии.`,actor:actor.id,choices:[
  {label:'Поддержать Хартию',hint:'дорога к автономии становится явной',effect:{autonomy:14,metropoleFavor:-9,legitimacy:6,locals:7,workers:5}},
  {label:'Положить проект в сейф',hint:'сохранить двусмысленность',effect:{autonomy:4,metropoleFavor:2,authority:5,actorRel:4}},
  {label:'Передать список подписантов Аурелии',hint:'лояльность ценой доверия',effect:{metropoleFavor:13,legitimacy:-10,locals:-10,workers:-8,suspicion:-4}}
 ]}
};return templates[id]}
function applyEventEffect(s,e,actorId){const maps={workers:'workers',planters:'planters',merchants:'merchants',garrison:'garrison',locals:'locals',bureaucrats:'bureaucrats'};for(const [k,v] of Object.entries(e)){if(k==='delayed'){s.delayed.push({...v,due:s.turn+v.after});continue}if(k==='actorRel'){const c=s.characters.find(x=>x.id===actorId);if(c)c.relation+=v;continue}if(maps[k]){s.factions[maps[k]].loyalty+=v;continue}if(k==='unrest'||k==='control'||k==='disease'||k==='production'||k==='scar'){s.districts.forEach(d=>d[k]+=v);continue}if(k in s)s[k]+=v}}
export function resolveEvent(s,index){if(!s.event)return false;const ch=s.event.choices[index];if(!ch)return false;applyEventEffect(s,ch.effect,s.event.actor);const actor=s.characters.find(c=>c.id===s.event.actor);if(actor){actor.memory.unshift({turn:s.turn,text:`Вы выбрали «${ch.label}» во время кризиса «${s.event.title}».`});actor.memory=actor.memory.slice(0,4)}s.history.push({turn:s.turn,type:'event',text:`${s.event.title}: ${ch.label}`});s.news.unshift(`${s.event.title}: ${ch.label}.`);s.lastAction=ch.label;s.event=null;normalize(s);return true}
function delayedTick(s){const due=s.delayed.filter(x=>x.due<=s.turn);s.delayed=s.delayed.filter(x=>x.due>s.turn);for(const d of due){if(d.kind==='blackmail'){s.corruption-=6;s.authority+=8;s.news.unshift('Старый информатор передал копии тайной бухгалтерии.')}if(d.kind==='fever_return'){s.districts.forEach(x=>x.disease+=8);s.legitimacy-=5;s.news.unshift('Скрытая лихорадка вернулась уже в трёх районах.')}if(d.kind==='imperial_bill'){s.metropoleFavor+=4;s.autonomy-=4;s.treasury-=8;s.news.unshift('Аурелия прислала счёт за «добровольную» защиту пролива.')}if(d.kind==='audit_echo'){s.suspicion+=8;s.metropoleFavor-=5;s.news.unshift('Один из ревизоров запросил оригиналы портовых книг.')}}}
function aiTick(s,r){for(const [id,f] of Object.entries(s.factions)){const drift=range(r,-2.5,2.5)+(s.legitimacy-50)*.015;f.loyalty+=drift;if(f.loyalty<30)f.power+=range(r,.2,1.6);if(f.loyalty>70)f.power+=range(r,-.4,.8)}for(const c of s.characters){const faction=s.factions[c.faction];c.relation+=(faction.loyalty-50)*.025+range(r,-1.2,1.2);if(c.traits.some(t=>t.id==='ambitious')&&c.relation<38)c.influence+=1.2;if(c.traits.some(t=>t.id==='venal'))s.corruption+=.35;if(c.relation<25&&c.influence>65&&r()<.09){s.suspicion+=4;s.legitimacy-=3;s.news.unshift(`${c.name} собирает собственный круг сторонников.`)}}}
function economyTick(s,r){const infra=avg(s,'infra'),prod=avg(s,'production'),disease=avg(s,'disease');const output=(prod*.38+infra*.18+s.trade*.24-disease*.16-s.unrest*.12)/6.6;const policy=s.imperialPolicy==='mercantile'?1.1:s.imperialPolicy==='austerity'?.92:1;const revenue=output*policy*(1-s.corruption/180);s.treasury+=revenue-6.0-s.debt*.014;s.foodStock+=(avg(s,'food')-48)*.08-s.population*.012;s.foodPrice+=(48-s.foodStock)*.035+s.externalPressure*.012-range(r,.5,1.4);s.trade+=(infra-45)*.012-s.externalPressure*.025+range(r,-1.3,1.3);s.garrisonPay+=(s.treasury>45?1.2:-2.2);s.authority+=3.8+(s.legitimacy-50)*.025;s.debt+=s.treasury<0?8:0;if(s.treasury<0)s.treasury=0;for(const d of s.districts){const before=d.unrest;d.unrest+=(s.foodPrice-50)*.045+(s.inequality-50)*.025+(d.disease-30)*.02-(s.legitimacy-50)*.018-d.control*.006;d.disease+=(40-d.infra)*.018+(s.foodPrice-55)*.015-range(r,.3,.8);d.production+=(d.infra-40)*.012-d.unrest*.01-d.disease*.006+range(r,-.5,.7);d.wealth+=(d.production-50)*.018+(s.trade-50)*.01;d.pop*=1+(d.food-45)*.00045-d.disease*.00055-d.unrest*.00025;d.trend=round(d.unrest-before)}s.corruption+=(s.treasury>90?.25:0)+(100-s.control)*.006-range(r,.1,.45);s.inequality+=(s.trade-50)*.004+(avg(s,'wealth')-45)*.006-range(r,.08,.3);s.externalPressure+=range(r,-1.5,1.8)+(s.imperialStability<40?.5:0)}
function imperialTick(s,r){if(s.turn%6!==0)return;s.imperialStability+=range(r,-7,5);if(s.preset==='collapse')s.imperialStability-=4;const policies=['austerity','mercantile','reform','hawkish'];if(r()<.48){s.imperialPolicy=pick(r,policies);s.news.unshift(`В Аурелии сменился курс: ${policyLabel(s.imperialPolicy)}.`)}if(s.imperialStability<32){s.autonomy+=6;s.externalPressure+=7;s.news.unshift('Метрополия занята собственным кризисом. Велория неожиданно получила пространство для манёвра.')}}
export function policyLabel(p){return({austerity:'жёсткая экономия',mercantile:'экспорт любой ценой',reform:'реформы и права',hawkish:'военная мобилизация'})[p]||p}
export function submitDispatch(s,tone){if(s.turn%3!==0||s.dispatchDone===s.turn)return{ok:false};const actual=Math.round((s.treasury+s.trade+s.legitimacy+(100-s.unrest))/4);let reported=actual,delta=0;if(tone==='polish'){reported+=16;delta=12;s.metropoleFa