(h=>`<div class="news-item"><small>ход ${h.turn}</small><br>${esc(h.text)}</div>`).join('')}</div></div>`}
function paneMarkup(){ return `<section class="desk">${metricsMarkup()}<div class="tabs" data-ui>${[['district','Район'],['reports','Донесения'],['decrees','Указы'],['people','Люди']].map(([id,l])=>`<button class="tab ${tab===id?'active':''}" data-tab="${id}" data-native-press>${l}</button>`).join('')}</div><div class="pane" data-touch-action="pan-y">${tab==='district'?districtPane():tab==='reports'?reportsPane():tab==='decrees'?decreePane():peoplePane()}</div></section>`; }
function gameScreen(){const canAdvance=!state.event&&!needsDispatch(state)&&!state.ending;return `<section class="screen game">
  <header class="topbar" data-ui><div class="brand"><a href="../../" class="icon-btn" data-app-control data-native-press style="display:grid;place-items:center;text-decoration:none">PW</a><strong>VELORIA</strong><span>MANDATE</span></div><div class="top-meta"><b>${esc(monthLabel(state))}</b><small>месяц ${state.turn} / 36 · полномочия ${fmt(state.authority)}</small></div><button class="icon-btn" id="pause" data-native-press>Меню</button></header>
  <div class="game-body">${mapMarkup()}${paneMarkup()}</div>
  <footer class="bottom" data-ui><div class="ticker"><span class="seal">V</span><p>${esc(state.news[0]||'Мандат продолжается.')}</p></div><button class="advance" id="advance" data-native-press ${canAdvance?'':'disabled'}>${needsDispatch(state)?'ДЕПЕША':state.event?'КРИЗИС':state.ending?'ИТОГ':'Следующий месяц'}<small>${state.decreeTurn===state.turn?'указ принят':'указ необязателен'}</small></button></footer>
 </section>`;}

function eventOverlay(){const e=state.event; if(!e)return ''; const actor=state.characters.find(c=>c.id===e.actor);return `<section class="overlay" data-ui><div class="sheet wide"><div class="eyebrow">Кризис / ${esc(monthLabel(state))}</div><h2>${esc(e.title)}</h2>${actor?`<div class="smallcaps">Докладчик: ${esc(actor.name)} · отношение ${fmt(actor.relation)}</div>`:''}<p class="event-copy">${esc(e.text)}</p><div class="choice-list">${e.choices.map((c,i)=>`<button class="choice" data-choice="${i}" data-native-press><b>${esc(c.label)}</b><small>${esc(c.hint)}</small></button>`).join('')}</div></div></section>`}
function dispatchOverlay(){const actual=Math.round((state.treasury+state.trade+state.legitimacy+(100-state.unrest))/4);return `<section class="overlay" data-ui><div class="sheet wide"><div class="eyebrow">Квартальная депеша в Аурелию</div><h2>Какую Велорию увидит империя?</h2><p>Метрополия не получает ваши внутренние показатели напрямую. Она получает <b>документ</b>. Ложь может купить поддержку и время, но расхождение между бумагой и реальностью копится.</p><div class="dispatch-meter"><div><span>Ваш реальный сводный индекс</span><b>${actual}</b></div><div><span>Риск аудита сейчас</span><b>${fmt(state.auditRisk)}%</b></div></div><div class="dispatch-options"><button data-dispatch="truth" data-native-press><b>Отправить честно</b><small>Меньше подозрений и расхождения. Без халявы.</small></button><button data-dispatch="polish" data-native-press><b>Отполировать цифры</b><small>Империя довольнее. Но потом придут люди с копиями старых депеш.</small></button><button data-dispatch="alarm" data-native-press><b>Сгустить краски</b><small>Можно выбить экстренную помощь, потеряв политический вес.</small></button></div></div></section>`}
function pauseOverlay(){return `<section class="overlay" data-ui><div class="sheet"><div class="eyebrow">Пауза</div><h2>Губернаторский кабинет</h2><div class="scoreboard"><div><span>Ход</span><b>${state.turn}/36</b></div><div><span>Счёт</span><b>${state.score}</b></div><div><span>Seed</span><b style="font-size:13px">${esc(state.seed)}</b></div><div><span>Автосейв</span><b>✓</b></div></div><div class="pause-links"><button class="btn primary" id="closePause" data-native-press>Вернуться к мандату</button><button class="btn ghost" id="showChronicle" data-native-press>Хроника</button><button class="btn ghost" id="showRules" data-native-press>Как работает игра</button><button class="btn ghost" id="toggleSound" data-native-press>${audio.enabled?'Звук: включён':'Звук: выключен'}</button><button class="btn ghost" id="restart" data-native-press>Новая партия</button><a class="btn ghost" href="../../" data-app-control data-native-press style="text-decoration:none;text-align:center">Вернуться в Pocket Works</a></div></div></section>`}
function rulesOverlay(){return `<section class="overlay" data-ui><div class="sheet rules"><div class="eyebrow">Полевая инструкция</div><h2>Управлять придётся не цифрами, а последствиями.</h2><h3>Каждый месяц</h3><ul><li>Посмотрите карту и донесения.</li><li>При желании подпишите <b>один</b> крупный указ.</li><li>Перейдите к следующему месяцу: экономика, районы, фракции и люди действуют сами.</li><li>Если возник кризис — примите политическое решение. Последствия могут вернуться позже.</li></ul><h3>Каждый квартал</h3><p>Вы отправляете депешу в Аурелию. Честность снижает риск аудита; приукрашивание покупает благосклонность; тревожная депеша может выбить деньги. Ни один вариант не бесплатный.</p><h3>Чего добиваться</h3><p>К концу 36 месяцев можно прийти к автономной хартии, имперской карьере, торговой республике или железному протекторату. Можно также быть отозванным, разорить колонию или потерять реальную власть.</p><button class="btn primary" id="closeRules" data-native-press>Понятно</button></div></section>`}
function chronicleOverlay(){return `<section class="overlay" data-ui><div class="sheet wide"><div class="eyebrow">Архив губернатора</div><h2>Хроника этой Велории</h2><div class="news-list">${state.history.slice().reverse().map(h=>`<div class="news-item"><small>Ход ${h.turn}</small><br>${esc(h.text)}</div>`).join('')||'<p>Архив пока пуст.</p>'}</div><button class="btn primary" id="closeChronicle" style="margin-top:12px" data-native-press>Закрыть архив</button></div></section>`}
function endingOverlay(){const e=state.ending;return `<section class="overlay" data-ui><div class="sheet"><div class="ending-stamp">${e.type==='loss'?'Мандат прекращён':e.type==='neutral'?'Мандат завершён':'Итог мандата'}</div><h2>${esc(e.ti