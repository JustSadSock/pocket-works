function setupEvents() { document.querySelector('#playBtn').addEventListener('click', () => { playing = !playing; syncUI(); }); document.querySelector('#stepBtn').addEventListener('click', () => { playing = false; simulateOne(); syncUI(); }); document.querySelector('#speedBtn').addEventListener('click', () => { const values = [.25, .5, 1, 2, 4]; speed = values[(values.indexOf(speed) + 1) % values.length]; syncUI(); }); document.querySelector('#rewindBtn').addEventListener('click', rewind); document.querySelector('#undoBtn').addEventListener('click', undo); document.querySelector('#redoBtn').addEventListener('click', redo); document.querySelector('#gravityBtn').addEventListener('click', () => { gravity = (gravity + 1) % 4; navigator.vibrate?.(settings.haptics ? 20 : 0); syncUI(); }); document.querySelector('#cameraBtn').addEventListener('click', () => { panMode = !panMode; selectedTool = panMode ? 'pan' : 'brush'; syncUI(); }); document.querySelector('#resetCameraBtn').addEventListener('click', fitCamera); document.querySelector('#toolsBtn').addEventListener('click', () => openSheet('#toolsSheet')); document.querySelector('#libraryBtn').addEventListener('click', () => { renderLibrary(); openSheet('#librarySheet'); }); document.querySelector('#synthesisBtn').addEventListener('click', () => { renderSynthesis(); populateRuleSelects(); openSheet('#synthesisSheet'); }); document.querySelector('#inspectorBtn').addEventListener('click', () => openSheet('#inspectorSheet')); document.querySelector('#layersBtn').addEventListener('click', () => openSheet('#layersSheet')); document.querySelector('#saveBtn').addEventListener('click', saveWorld); document.querySelector('#snapshotBtn').addEventListener('click', () => { const a = document.createElement('a'); a.download = `veshchestvo-${Date.now()}.png`; a.href = canvas.toDataURL('image/png'); a.click(); }); document.querySelector('#exportBtn').addEventListener('click', exportBundle); document.querySelector('#importBtn').addEventListener('click', () => document.querySelector('#importFile').click()); document.querySelector('#importFile').addEventListener('change', e => e.target.files[0] && importBundle(e.target.files[0])); document.querySelector('#copyBtn').addEventListener('click', copySelection); document.querySelector('#pasteBtn').addEventListener('click', () => { selectedTool = 'paste'; syncUI(); }); document.querySelector('#materialSearch').addEventListener('input', e => renderMaterials(e.target.value)); document.querySelector('#brushSize').addEventListener('input', e => { brushSize = +e.target.value; document.querySelector('#brushValue').textContent = brushSize; }); document.querySelectorAll('[data-category]').forEach(b => b.addEventListener('click', () => { activeCategory = b.dataset.category; document.querySelectorAll('[data-category]').forEach(x => x.classList.toggle('active', x === b)); renderMaterials(document.querySelector('#materialSearch').value); })); document.querySelectorAll('[data-layer]').forEach(b => b.addEventListener('click', () => { activeLayer = b.dataset.layer; syncUI(); closeSheet('#layersSheet'); })); document.querySelectorAll('[data-close]').forEach(b => b.addEventListener('click', () => closeSheet(`#${b.dataset.close}`))); document.querySelector('#synSave').addEventListener('click', saveSynthetic); document.querySelectorAll('#synthesisSheet input,#synthesisSheet select').forEach(el => el.addEventListener('input', () => { document.querySelector('#synWarning').hidden = true; updateSynPreview(); })); document.querySelector('#soundToggle').addEventListener('change', e => { settings.sound = e.target.checked; saveAutosave(); }); document.querySelector('#hapticToggle').addEventListener('change', e => { settings.haptics = e.target.checked; saveAutosave(); }); document.querySelector('#licensesBtn').addEventListener('click', () => openSheet('#licensesSheet')); window.addEventListener('resize', () => { resize(); fitCamera(); }); document.addEventListener('visibilitychange', () => { if (document.hidden)
    playing = false; syncUI(); saveAutosave(); }); window.addEventListener('beforeunload', saveAutosave); }
function init() { resize(); const savedCustom = JSON.parse(localStorage.getItem(`${STORAGE}:materials`) || '[]'); if (savedCustom.length)
    installCustomMaterials(savedCustom); setupEvents(); renderTools(); renderMaterials(); renderInspector(); renderLibrary(); populateRuleSelects(); document.querySelector('#brushSize').value = brushSize; document.querySelector('#brushValue').textContent = brushSize; document.querySelector('#soundToggle').checked = settings.sound; document.querySelector('#hapticToggle').checked = settings.haptics; if (!loadAutosave())
    setupVolcano(); syncUI(); if (!SMOKE_MODE && 'serviceWorker' in navigator)
    navigator.serviceWorker.register('./sw.js').catch(() => { }); if (SMOKE_MODE) {
    playing = false;
    simulateOne();
    render();
    document.documentElement.dataset.ready = 'true';
}
else
    requestAnimationFrame(frame); }
if (SMOKE_MODE)
    window.__VESHCHESTVO_TEST__ = { materials, scenes, tasks, simulateOne, encodeState, applyState, setupVolcano, setupBlank, countMat, maxTemp, maxPressure };
init();
