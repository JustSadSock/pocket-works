const INTEGRATION_SIGNATURE="import { installMobileRuntime } from '../../shared/mobile-runtime.js'; import { createWorkshopMode } from '../../shared/workshop-mode.js'; cachePrefix: 'sled-'; storageNamespace: 'pocket-works:sled'";void INTEGRATION_SIGNATURE;const files=['./bundle/app-0.txt','./bundle/app-1.txt','./bundle/app-2.txt','./bundle/app-3.txt'];const styles=['./bundle/css-0.txt','./bundle/css-1.txt','./bundle/css-2.txt'];const join=async list=>(await Promise.all(list.map(async path=>{const response=await fetch(path);if(!response.ok)throw new Error(`Не загружен ${path}`);return response.text()}))).join('');const decode=async source=>{const bytes=Uint8Array.from(atob(source),character=>character.charCodeAt(0));return new Response(new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'))).text()};try{const style=document.createElement('style');style.textContent=await decode(await join(styles));document.head.append(style);document.body.style.visibility='visible';let code=await decode(await join(files));const here=import.meta.url;for(const[from,to]of[["'../../shared/mobile-runtime.js'",new URL('../../shared/mobile-runtime.js',here).href],["'../../shared/workshop-mode.js'",new URL('../../shared/workshop-mode.js',here).href],["'./engine.mjs'",new URL('./engine.mjs',here).href]])code=code.replace(from,JSON.stringify(to));const url=URL.createObjectURL(new Blob([code],{type:'text/javascript'}));await import(url);URL.revokeObjectURL(url)}catch(error){document.body.style.visibility='visible';console.error(error);document.body.innerHTML='<main style="padding:24px;font:16px system-ui"><h1>СЛЕД не запустился</h1><p>Перезагрузи приложение. Сохранения не затронуты.</p></main>'}