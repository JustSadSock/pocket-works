{
  "name": "@pocket-works/dead-air-13",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vitest run --config vite.config.ts && tsc --noEmit -p tsconfig.json && vite build --config vite.config.ts && node ../../scripts/promote-enhanced-build.mjs dead-air-13",
    "test": "vitest run --config vite.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
